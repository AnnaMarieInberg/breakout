
function setup() {
  
  createCanvas(1280, 720);
  background('blue');
}
 
class SpelElement {
    x;
    y;


constructor (_x,_y)
    _x=x;
    _y=y;
}

class Ball extends SpelElement {
    speedX;
    speedY;

constructor(_speedX,_speedY)
    _speedX=speedX;
    _speedY=speedY;
}
